import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBackground = Color(
    0xFF1A2C42,
  ); // Dark blue from header/sidebar
  static const Color accentBlue = Color(
    0xFF2D8BCA,
  ); // Blue from buttons/active links
  static const Color lightGreyBackground = Color(
    0xFFF3F4F6,
  ); // Light gray from main content
  static const Color cardBackground = Colors.white;
  static const Color textDark = Color(
    0xFF374151,
  ); // Dark gray for headings/text
  static const Color textGrey = Color(
    0xFF6B7280,
  ); // Medium gray for labels/snippets
  static const Color lightBorder = Color(0xFFE5E7EB); // Light border color

  // Status colors
  static const Color statusNormalBg = Color(0xFFE6FFFA); // Green-100
  static const Color statusNormalText = Color(0xFF38A169); // Green-800
  static const Color statusAlertBg = Color(0xFFFEE2E2); // Red-100
  static const Color statusAlertText = Color(0xFFE53E3E); // Red-600
  static const Color statusInfoBg = Color(
    0xFFE0F2F7,
  ); // Blue-50 (custom for info)
  static const Color statusInfoText = Color(
    0xFF2B6CB0,
  ); // Blue-600 (custom for info)
  static const Color errorRed = Color(0xFFE53935);
  static const Color normalStatus = Colors.green;
  static const Color warningStatus = Colors.orange;
  static const Color criticalStatus = Colors.red;
}

